<!DOCTYPE html>
<html>
  <head>
    <title>KlukHouse</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <%= render "devise/shared/navbar" %>
    <main class="container mx-auto mt-28 px-5">
      <% if notice.present? %>
        <div class="mx-auto w-full max-w-md mb-6 rounded-xl bg-green-100 text-green-800 px-4 py-3 border border-green-200 text-center">
          <%= notice %>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="mx-auto w-full max-w-md mb-6 rounded-xl bg-red-100 text-red-800 px-4 py-3 border border-red-200 text-center">
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
