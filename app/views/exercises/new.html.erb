
<% content_for :title, "New exercise" %>

<div class="w-full max-w-3xl mx-auto">
  <!-- Header -->
  <%= render "shared/new_header", instance: @exercise, message: 'Add a new movement to your library 🐒', index_path: exercises_path %>

  <!-- Form card -->
  <div class="bg-white rounded-2xl shadow-sm border border-yellow-100 p-6">
    <%= render "form", exercise: @exercise %>
  </div>

  <!-- Bottom back (mobile friendly) -->
  <div class="mt-6 sm:hidden">
    <%= link_to "Back to exercises", exercises_path, class: "block text-center rounded-lg px-4 py-2 bg-yellow-100 text-yellow-900 font-semibold hover:bg-yellow-200 transition cursor-pointer" %>
  </div>
</div>
