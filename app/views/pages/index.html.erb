<div class="w-full max-w-5xl mx-auto">

  <!-- Hero -->
  <div class="text-center mb-10">
    <h1 class="text-5xl font-extrabold text-yellow-900">
      Welcome to KľukHouse 🐒
    </h1>

    <p class="mt-4 text-lg text-yellow-800">
      Plan your workouts. Track your progress. Go bananas 🍌
    </p>

    <div class="mt-6 flex flex-col sm:flex-row justify-center gap-3">
      <% if user_signed_in? %>
        <%= link_to "My exercise plans", exercise_plans_path, class: "rounded-lg px-6 py-3 bg-green-500 hover:bg-green-600 text-white font-semibold shadow-sm transition cursor-pointer" %>
      <% else %>
        <%= link_to "Get started", new_user_registration_path, class: "rounded-lg px-6 py-3 bg-green-500 hover:bg-green-600 text-white font-semibold shadow-sm transition cursor-pointer" %>
        <%= link_to "Log in", new_user_session_path, class: "rounded-lg px-6 py-3 bg-yellow-100 hover:bg-yellow-200 text-yellow-900 font-semibold transition cursor-pointer" %>
      <% end %>
    </div>
  </div>

  <!-- Popular exercises -->
  <%= render "most_popular_exercises" %>
</div>
