<% content_for :title, "New exercise plan" %>

<div class="w-full max-w-3xl mx-auto">
  <%= render "shared/new_header", instance: @exercise_plan, message: 'Build a plan manually or generate one with AI 🍌', index_path: exercise_plans_path %>

  <div class="bg-white rounded-2xl shadow-sm border border-yellow-100 p-6">
    <%= render "form", exercise_plan: @exercise_plan %>
  </div>
</div>
